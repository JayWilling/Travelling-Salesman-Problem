#include "directedGraphCopy.h"

// We will have to update the graph to be undirected and for each node to store a set of points

template <typename vertex> directed_graph<vertex> tsp_method_one() {

    return 0;
}

template <typename vertex> directed_graph<vertex> tsp_method_two() {

    return 0;
}